function calculate() {
    const cpuIntel = +document.optionIntelCpu.value,
          cpuAmd = +document.value;
    s0 = vid + moth + coolers + opMemory + cpu;
    s1 = s0 * 1.15;
    otv = Math.round(s1);
    document.f.r.value = otv;
}